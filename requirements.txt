pandas==2.2.1
numpy==1.26.4
scikit-learn==1.4.0
mlflow==2.11.1
boto3==1.34.34          # Only needed if storing MLflow artifacts in S3
python-dotenv==1.0.1    # Optional, for local env var loading
requests==2.31.0        # If your extract step uses HTTP
kagglehub
